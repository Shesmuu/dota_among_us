class MinigameGrave extends Minigame {
	constructor() {
		super( "#GraveGame", "GraveContainer" )


		
		this.heroes = []

		for ( let i = 5; i >= 0; i-- ) {
			//this.heroes.push( new OsuCircle( this.container, circle => this.Clicked( circle ) ) )
		}
	}

	Clicked() {

	}

	Update( now ) {
		
	}
}

